export interface NoteData {
  id: number;
  title: string;
  text: string;
  favorite: boolean;
  color: string;
}

export interface FormData {
  title: string;
  text: string;
}

export interface ObjectColor {
  color: string;
}
